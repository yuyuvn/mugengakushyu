<%= form_for(@wrong_kanji) do |f| %>
  <% if @wrong_kanji.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wrong_kanji.errors.count, "error") %> prohibited this wrong_kanji from being saved:</h2>

      <ul>
      <% @wrong_kanji.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :times %><br>
    <%= f.number_field :times %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :kanji_id %><br>
    <%= f.text_field :kanji_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
